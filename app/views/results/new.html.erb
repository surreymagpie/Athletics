<%= provide(:title, "Add single result") %>
<div class="medium-6 small-centered columns">
<h1>Add a single result</h1>
<p>This form is only used to add a single missing result into a given race, probably as a result of a query.</p>
<%= render 'form' %>
</div>